<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
      <!--  <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>-->
        <item name="colorPrimary">#523738</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">#303F9F</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">#4682b4</item>



    </style>
    <color name="date_set_btn_color">#4682b4</color>
    <color name="dim_foreground_disabled_material_dark1">#dcdcdc</color>
    <style name="SplashTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="LoginTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/start_up</item>
    </style>

    <style name="ButtonTheme" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="android:colorBackground">#AABBCC</item>
    </style>

</resources>
